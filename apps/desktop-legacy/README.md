# desktop-legacy (Tauri + React)

**Deprecated** — maintenance mode until Electron + Angular parity in `apps/desktop`.

```bash
pnpm dev:legacy
```

Release: `.github/workflows/release.yml` (Tauri). Target: `release-desktop.yml` (Electron).
