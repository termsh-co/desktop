# desktop (Electron + Angular)

Target termsh desktop client.

```bash
pnpm dev:desktop     # from repo root
pnpm electron:start  # from this package
```

Version source: `package.json` → CalVer `0.0.0` until first release.

Native: `@termsh/core-napi` (build: `pnpm build:napi`).
